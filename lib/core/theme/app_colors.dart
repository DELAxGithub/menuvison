import 'package:flutter/material.dart';

class AppColors {
  // Primary colors
  static const Color primary = Color(0xFF007AFF);
  static const Color background = Colors.white;
  static const Color surface = Color(0xFFF2F2F7);
  
  // Text colors
  static const Color textPrimary = Colors.black87;
  static const Color textSecondary = Color(0x993C3C43);
  
  // Other colors
  static const Color divider = Color(0xFFC6C6C8);
  static const Color error = Color(0xFFFF3B30);
  
  // Overlay colors for OCR
  static const Color textOverlay = Colors.white;
  static const double textOverlayOpacity = 0.5;
}